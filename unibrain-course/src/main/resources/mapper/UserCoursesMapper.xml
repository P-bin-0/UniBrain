<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bin.mapper.UserCoursesMapper">
    <select id="getShowUserCourses" resultType="com.bin.dto.vo.UserCoursesVO">
        SELECT
            uc.id,
            u.`name` AS user_name,
            c.`name` AS courses_name,
            uc.`week`,
            uc.day_hour,
            uc.room,
            uc.teacher
        FROM user_courses uc
        LEFT JOIN `user` u ON uc.user_id = u.id
        LEFT JOIN courses c ON uc.courses_id = c.id
    </select>
</mapper>